{
  "name": "niakofa-legacy-engine",
  "version": "0.1.0",
  "description": "Core Living World Engine for Niakofa Legacy: WorldState, TimeManager, WeatherManager, data-driven AnimationController, and a frame-accurate Combat system (hitboxes/hurtboxes/damage/knockback). Framework-agnostic core with a PixiJS v8 rendering adapter.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/__tests__/*.test.ts",
    "demo:headless": "node --import tsx src/examples/headless-simulation.ts"
  },
  "dependencies": {
    "pixi.js": "^8.6.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.1",
    "@types/node": "^22.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
