# Niakofa reference materials

This directory records source material used during production-readiness work.
Uploaded files remain under `attached_assets/` so the original filenames and
contents are preserved.

- `attached_assets/Pasted-Configure-production-Stripe-and-Redis-BullMQ-Add-focuse_1788135247289.txt`
  — session brief for production Stripe, Redis/BullMQ, and member-scoped
  Community Pool route coverage.

No ZIP archive was present in the upload for this pass. Credentials and
connection URLs must stay in the workspace or deployment secret manager and
must not be copied into reference files.