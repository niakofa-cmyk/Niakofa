Issues I noticed:
1. useAnimationPreference first-paint race: The hook uses useState(() => readPref()) for lazy init, then useEffect(() => applyPref(animEnabled), [animEnabled]) to apply.
The useEffect runs after the first render, so on iOS with Reduce Motion enabled and a stored override, there's a flash where the bird renders without the animation attribute for one frame before the effect fires and adds it—causing a brief visual flicker. I need to apply the preference synchronously at module initialization instead, calling applyPref(readPref()) at the module level so it runs immediately when imported, before any render happens.
For the accessibility card, the description saying "Matching your OS Reduce Motion setting — tap to always show animations" is a bit confusing when the user doesn't actually have Reduce Motion enabled, since the toggle would just set an attribute that has no effect. The card should probably clarify that the toggle only matters when OS Reduce Motion is on, though the current description isn't technically broken—it's just not as clear as it could be.
The isGliding prop being used for ellipse dimensions on lines 539 and 549 is intentional since those are structural body shape changes rather than CSS visual effects, so that's correct. The comment in useAnimationPreference.ts is slightly outdated though—it says only Phase 3 uses the reduced-motion guard, but now all 5 blocks use it. There's also a test at line 301 checking that landingPhase='flying' correctly affects isMoving and isVisuallyGliding that I should verify covers the necessary cases.
The most critical issue is a potential first-paint race condition on iOS when stored preferences and Reduce Motion are both enabled—there's a one-frame flash where animations get suppressed before the data-bird-anim="enabled" attribute is set. I need to apply the preference at module initialization time rather than waiting for useEffect, so the HTML attribute is already in place before React renders anything. I should also check that the bird-test.tsx Phase 3 driving-speed glide cards have the correct data-speed attribute set to "driving". I need to update the JSDoc to clarify that all blocks are now guarded, then verify the driving-speed elongation selector is properly defined in the CSS, and trace through how the data-speed attribute gets set in the SVG component to ensure the end-to-end flow works correctly.



Let me analyze what I've found and determine the concrete fixes needed:
1. First-paint race in useAnimationPreference: The hook uses useEffect to apply the DOM attribute, but useEffect runs after the first render. So on iOS with stored preference + Reduce Motion ON, there's a one-frame flash. Fix: call applyPref(readPref()) at module level so it runs at import time, before any render.
2. data-gliding + data-speed="driving" elongation CSS: I confirmed the selector exists at line 5022:
    * When isVisuallyGliding=true (14 m/s), both data-gliding="true" AND data-speed="driving" are set, so this rule fires and scales the body correctly. ✅
3. Missing data-speed="driving" elongation for neck: The CSS has neck elongation for data-gliding="true" alone, but it's missing the combined selector for when both gliding and driving are active.
4. AccessibilityCard description clarity: The toggle description "Matching your OS Reduce Motion setting" is misleading when the user doesn't have Reduce Motion enabled — it suggests their animations are already reduced when they're not. The card should either say "Only active when OS Reduce Motion is on" or hide conditionally.
Looking at the logic more carefully: when the OS has Reduce Motion OFF, the @media (prefers-reduced-motion: reduce) block never fires, so the data-bird-anim attribute doesn't matter and the toggle becomes confusing.
5. useAnimationPreference docstring: The comment still references "Phase 3 reduced-motion CSS block" when it should say "ALL reduced-motion blocks".
I need to fix these issues, starting with adding a module-level applyPref(readPref()) call in useAnimationPreference.ts...
Actually, let me reconsider that test case at line 301 where landingPhase='flying' with a speed of 14 — I need to verify the edge case where speed is 5 m/s or lower to ensure isVisuallyGliding behaves correctly.
Looking at the existing tests, I don't see coverage for the scenario where speed is 5 m/s with landingPhase='flying' — isMoving should be true but isVisuallyGliding should be false since it's below the 10 m/s threshold. That's a gap I should fill.
I also want to add a test for computeLeanDeg when driving at speed with isGliding=false but isVisuallyGliding=true — that should return 15° rather than the 12° airplane glide lean. Let me trace through the computeLeanDeg logic to make sure it's handling the driving case correctly.
Now I'm mapping out the fixes I need to make: first, I'll add the synchronous preference application at module init in useAnimationPreference.ts to prevent the flash, then improve the AccessibilityCard to show the OS Reduce Motion state with contextual descriptions, update the docstring, and add those missing test cases. Adding OS state indicator, updating the docstring, and writing a test case for this scenario. I should also check if other files have @media prefers-reduced-motion blocks that need the same guard logic.
I've found concrete issues. Let me now check for reduced-motion in other files and the driving-neck elongation gap simultaneously:


Found more issues — KindnessImpactRing.tsx and TurnArrowHUD.tsx both read prefers-reduced-motion directly via matchMedia, bypassing the user's override toggle entirely. Reading them now to assess the scope: