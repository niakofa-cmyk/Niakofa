Performance: Throttle GPS, simplify layers at low zoom.
A persistent "you have an active job" reminder while browsing. Right now, a helper mid-job can still navigate to the Nearby tab and see the full browse/claim UI, with nothing reminding them they're already committed elsewhere. A small, dismissible banner — "Job in progress at [address] · tap to return" — would prevent the confusing state of browsing new requests while one is already active.

Add A legend for what the pins mean in Helper Dashboard. Request pins already carry real meaning through color and styling — emergency vs. high-urgency vs. normal — but there's nothing on screen explaining that to a new user. Someone opening the map for the first time has no way to know a red pin means something different from a regular one without tapping every single one to find out Add A legend for what the pins mean in Helper Dashboard

But there's one real problem the update introduced (or rather, didn't account for):
The helper-mode request list now covers the controls you just cleaned up. BottomSheet.tsx renders at bottom-16, z-20, up to max-h-[55vh] — and it has a drag-handle shown (the little pill at the top) but no actual drag/collapse logic wired to it (no drag prop, no collapsed state, no touch handler). So the moment a Helper is online with open requests nearby — which is the single most common state for someone in Helper Mode — that sheet sits on top of and hides both the new Filters/Layers row (bottom-32, z-10) and the Orientation/Recenter row (bottom-48, z-10), since z-20 beats z-10 and 55vh comfortably covers 192px. You consolidated four buttons into two, but a Helper with requests nearby can't reach either of them without first dismissing the whole request list.
Two ways I'd fix that, in order of preference:
1. Make the grab handle real. Give the sheet an actual collapsed/expanded state — collapsed shows just the handle + "12 open requests" header (roughly 72px), expanded is the current 55vh. Default to collapsed, expand on tap or drag-up. This also just makes the sheet nicer to use generally — right now a helper can't glance at the map at all while it's open.
2. If you want the quick fix first: move Filters/Layers and Orientation/Recenter to z-30 so they render above the sheet regardless, and keep them clear of it by only extending the sheet to max-h-[40vh] instead of 55vh — a smaller, safer patch, but the collapse behavior is the actual right long-term fix since the sheet blocking the whole map view is its own usability problem even ignoring the buttons underneath it.


Collapsed by default. The sheet now starts collapsed — just the drag handle + "Nearby Requests · 12 open" header peeking up (96px), leaving the Filters/Layers row and Orientation/Recenter row fully visible and tappable underneath. Tap the header, or drag up, to expand to the full 55vh list.
The grab handle is now real. It uses framer-motion's useDragControls with dragListener={false} on the sheet itself, so only the handle triggers a drag (the request list below keeps its own scroll — dragging a card won't fight with dragging the sheet). Drag past 40px or flick fast enough (velocity > 300) and it snaps to the other state; otherwise it springs back.
Emergencies always win. If an emergency request is already in the list when the sheet mounts, or one arrives while the sheet is collapsed, it force-expands automatically — this matches the "emergency bypasses everything" rule your AUDIT_SUMMARY already established for distance checks and notification preferences. A helper should never have an emergency sitting invisible behind a collapsed sheet.
Accessibility: both the handle and header are keyboard-operable (role="button", tabIndex, Enter/Space), aria-expanded reflects state, and the list content gets aria-hidden + pointer-events: none while collapsed so a screen reader or stray tap can't reach hidden cards.
Nothing else in the file changed — same request cards, same claim/details buttons, same payment badges.
One thing to verify on your end, since I can't run the actual mobile build here: confirm the collapsed peek height (96px) actually clears whatever's directly below it — the BottomNav — on your target devices; if BottomNav's height differs from what I saw referenced elsewhere in your notes (~64-98px with safe area), nudge COLLAPSED_PEEK_PX at the top of the file accordingly.
