# Nixpacks build configuration for Railway deployment

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[variables]
NODE_ENV = "production"
SERVE_FRONTEND = "true"
