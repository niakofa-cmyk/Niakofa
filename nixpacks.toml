providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["cd artifacts/pay-it-forward && npm install && npm run build"]

[start]
cmd = "cd artifacts/pay-it-forward && ./node_modules/.bin/serve dist -p $PORT"
