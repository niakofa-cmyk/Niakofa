[build]
builder = "railpack"

[deploy]
startCommand = "pnpm --filter @workspace/db run migrate && (node artifacts/nia-service/dist/index.js &) && node --enable-source-maps artifacts/api-server/dist/index.mjs"
